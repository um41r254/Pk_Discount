<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mid_banchers.pk_discount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pk_Discount">
        <activity android:name=".Clothes_tab"></activity>
        <activity android:name=".Shoe_tab" />
        <activity android:name=".filters" />
        <activity android:name=".Multiple_usage" />
        <activity android:name=".All_brands" />
        <activity android:name=".Trending" />
        <activity android:name=".Tester" />
        <activity android:name=".Product_detail" />
        <activity android:name=".Hot_deals" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>